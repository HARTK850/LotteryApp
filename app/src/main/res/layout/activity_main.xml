<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_background"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/editNames"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="הזן שמות מופרדים בפסיקים"
        android:textColorHint="#FFFFFF"
        android:textColor="#FFFFFF"
        android:backgroundTint="#80FFFFFF"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/btnPickFile"
        android:layout_margin="16dp" />

    <Button
        android:id="@+id/btnPickFile"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="בחר קובץ טקסט"
        android:background="@drawable/button_style"
        android:textColor="#FFFFFF"
        app:layout_constraintTop_toBottomOf="@id/editNames"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="16dp" />

    <Button
        android:id="@+id/btnDraw"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="הגרל זוכה"
        android:background="@drawable/button_style"
        android:textColor="#FFFFFF"
        app:layout_constraintTop_toBottomOf="@id/btnPickFile"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="16dp" />

    <Button
        android:id="@+id/btnNextWinner"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="זוכה נוסף"
        android:background="@drawable/button_style"
        android:textColor="#FFFFFF"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/btnDraw"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="16dp" />

    <TextView
        android:id="@+id/tvWinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="הזוכה הוא..."
        android:textSize="32sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/btnNextWinner"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_margin="32dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
